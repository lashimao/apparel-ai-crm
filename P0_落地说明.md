# NexFab CRM P0 落地说明

本说明对应 2026-08-13 继续补强后的工程骨架，目标是把“报价 → 订单 → 回款”从文档设计推进到可复用业务规则和 API 入口。

## 已补充内容

### 1. 工程结构

- `src/app/page.tsx`：工作台页面。
- `src/app/layout.tsx`：Next App Router 根布局。
- `src/server/ai/router.ts`：AI 路由层，含敏感数据和人工确认边界。
- `src/domain/salesCashRules.ts`：报价、订单、回款核心业务规则。
- `src/app/api/**/route.ts`：业务规则 API 入口。
- `prisma/schema.prisma`：Prisma schema 正式位置。
- `tests/salesCashRules.test.ts`：无需测试框架的业务规则验证。
- `workers/index.ts`：Worker 启动入口占位。

### 2. 已实现业务规则

- 报价核算：按成本、数量、目标毛利和分摊费用倒推建议价。
- 毛利底线：低于底线自动进入审批状态。
- 锁价限制：未人工锁价的报价明细不得发送。
- 报价发送校验：低毛利未审批、审批拒绝、过期、未锁价都会阻断。
- 报价转订单：只有客户已确认且审批条件满足的报价可以转订单。
- 回款识别：支持待回款、部分回款、已付款、已核销、逾期风险。
- 工作台待办：聚合报价审批、订单跟进、回款核销事项。

### 3. API 入口

- `POST /api/quotations/calculate`：计算报价。
- `POST /api/quotations/validate-send`：发送前校验。
- `POST /api/quotations/convert-to-order`：报价转订单草稿。
- `POST /api/payments/evaluate`：回款状态和风险判断。
- `POST /api/workbench/sales-cash-todos`：生成销售到回款待办。
- `GET /api/health`：健康检查。

### 4. 已验证项

已用 Node 内置 TypeScript strip 模式验证业务规则：

```bash
node --experimental-strip-types tests/salesCashRules.test.ts
```

结果：`salesCashRules tests passed`

已用 Prisma 6.19.3 验证 schema：

```bash
DATABASE_URL='postgresql://crm:crm_secret@localhost:5432/crm?schema=public' pnpm dlx prisma@6.19.3 validate --schema prisma/schema.prisma
```

结果：`The schema at prisma/schema.prisma is valid`

## 下一步建议

1. 安装依赖并启动本地 Next 服务。
2. 执行 Prisma migration，把模型真正落库。
3. 把 API 从纯规则返回改为读取/写入数据库。
4. 在报价页面接入 `calculate` 和 `validate-send`。
5. 在订单页面接入 `convert-to-order`。
6. 在财务页面接入 `payments/evaluate` 与核销动作。
7. 把所有正式状态变更写入 `AuditLog`。
